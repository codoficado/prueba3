server:
  port: 8087

spring:
  application:
    name: Resenas
  datasource:
    url: jdbc:mysql://localhost:3306/db_anfri_resenas
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  security:
    user:
      name: admin
      password: 12345